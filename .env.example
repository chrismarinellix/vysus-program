# Supabase Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env files with real credentials

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# Note: For this single-page HTML app, these are embedded in the HTML.
# The anon key is safe to expose (it's public), but RLS policies
# control actual data access.
#
# For production apps, consider:
# 1. Using a build process to inject environment variables
# 2. Hosting on Netlify with environment variables
# 3. Using Supabase Edge Functions for sensitive operations
